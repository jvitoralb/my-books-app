export type LoginCredentials = {
    email: string;
    password: string;
}

export type UserAuth = {
    token: string;
    expires: string;
}

export type User = {
    name: string;
    email: string;
}
